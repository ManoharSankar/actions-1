name: My first workflow 
on: push    

jobs:  
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout stage
          uses: actions/checkout@v4

        - name: List and Read files
          run: |
            chmod +x script.sh
            ./script.sh